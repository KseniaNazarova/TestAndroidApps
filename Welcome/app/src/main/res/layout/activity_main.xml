<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/welcomeLinearLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="net.ucoz.ksen.welcome.MainActivity"
    android:orientation="vertical"
    android:weightSum="2">

    <TextView
        android:text="@string/welcome"
        android:textAppearance="@android:style/TextAppearance.Holo.Large"
        android:id="@+id/welcomeTextView"
        android:textSize="@dimen/welcome_textsize"
        android:textColor="@color/welcome_textcolor"
        android:gravity="center"
        android:layout_gravity="center_horizontal"
        android:layout_weight="1"
        android:layout_width="wrap_content"
        android:layout_height="0dp" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        app:srcCompat="@drawable/snorlax"
        android:id="@+id/snorlaxImageView"
        android:layout_weight="1"
        android:layout_gravity="center"
        android:contentDescription="@string/snorlax_logo" />

</LinearLayout>
